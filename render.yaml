services:
  - type: worker
    name: ai-pomogator
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    autoDeploy: true
    envVars:
      - key: BOT_TOKEN
        sync: false   # значение задашь в панели Render (Environment)
